import React from 'react';
import { render} from 'react-dom';
import SubscriptionForm from '../../src';

const App = () => (
    <SubscriptionForm />
);

render(<App />, document.getElementById("root"));
